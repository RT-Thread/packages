config PKG_USING_HELLO
    bool "A hello package"
    default n

if PKG_USING_HELLO

    config PKG_HELLO_PATH
        string
        default "/packages/misc/hello"

    select PKG_HELLO_PATH

    config PKG_HELLO_VER
        string
        default "v1.0.0"

    select PKG_HELLO_VER

endif
