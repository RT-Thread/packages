
config PKG_USING_IPERF
    bool "IPerf: iperf-liked network performance tool"
    default n

if PKG_USING_IPERF

    config PKG_IPERF_PATH
        string
        default "/packages/tools/iperf"

    config PKG_IPERF_VER
        string
        default "v1.0.0"

endif
