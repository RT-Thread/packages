
config PKG_USING_RTI
    bool "RT-Thread Insight: probe tool for RT-Thread"
    default n

if PKG_USING_RTI

    config PKG_RTI_PATH
        string
        default "/packages/system/rti"

    config PKG_RTI_VER
        string
        default "v1.0.0"
        
endif
