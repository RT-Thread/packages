config PKG_USING_WICED
    bool "Wlan Driver from Wireless Internet Connectivity for Embedded Devices"
    default n

if PKG_USING_WICED

    config PKG_WICED_PATH
        string
        default "/packages/iot/WiFi/wiced"

    config PKG_WICED_VER
        string
        default "v1.0.0"
endif
