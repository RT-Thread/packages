menuconfig PKG_USING_NANOPB
    bool "nanopb: Protocol Buffers for Embedded Systems"
    default n

if PKG_USING_NANOPB

    config PKG_NANOPB_PATH
        string
        default "/packages/iot/nanopb"

    config PKG_NANOPB_VER
        string
        default "v1.0.0"

endif
